/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
        images : {
        domains : ['localhost', 'i.scdn.co', 't.scdn.co', 'charts-images.scdn.co', 'thisis-images.spotifycdn.com']
    }
};

export default nextConfig;
